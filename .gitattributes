# Track all assets with LFS
src/assets/* filter=lfs diff=lfs merge=lfs -text

# Exclude JavaScript files from LFS tracking
src/assets/*.js -filter -diff=lfs -merge=lfs text
src/assets/*.js -filter -diff=lfs -merge=lfs text
